{
    "build": {
        "buildCommand": "python manage.py collectstatic --noinput"
    },
    "deploy": {
        "startCommand": "python manage.py migrate && python -c \"import django; django.setup(); from django.db import connection; cursor = connection.cursor(); cursor.execute('ALTER TABLE healthcare_provider ALTER COLUMN slug TYPE varchar(200)'); cursor.execute('ALTER TABLE healthcare_location ALTER COLUMN slug TYPE varchar(200)'); print('Columns altered')\" && python manage.py loaddata healthcare/fixtures/zenthir_data.json && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
    }
}
