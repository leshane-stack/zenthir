{
    "build": {
        "buildCommand": "python manage.py collectstatic --noinput"
    },
    "deploy": {
        "startCommand": "python manage.py migrate && python manage.py seed_initial && python manage.py seed_providers && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
    }
}
